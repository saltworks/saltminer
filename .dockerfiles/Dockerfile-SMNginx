# syntax=docker/dockerfile:1

# Pin to current nginx stable release (1.28.3)
FROM nginx:1.28.3

# Copy SaltMiner nginx configuration files from the repo
# Source: github.com/saltworks/saltminer-setup/tree/develop/config/saltminer/nginx
COPY config/saltminer/nginx/nginx.conf    /etc/nginx/nginx.conf
COPY config/saltminer/nginx/saltminer.crt /etc/nginx/saltminer.crt
COPY config/saltminer/nginx/saltminer.key /etc/nginx/saltminer.key

# Restrict permissions on the private key
RUN chmod 600 /etc/nginx/saltminer.key

# Expose standard HTTP and HTTPS ports
EXPOSE 80 443
