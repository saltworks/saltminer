docs/

# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Playwright scaffolding - generated by `npm init playwright`, not used by this
# Python project.  Spec files are matched by pattern rather than ignoring tests/
# outright, so a future python test directory is not silently hidden.
/playwright.config.ts
/package.json
/package-lock.json
/.github/workflows/playwright.yml
**/*.spec.ts
